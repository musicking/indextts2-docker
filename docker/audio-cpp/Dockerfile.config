# syntax=docker/dockerfile:1.7
# Config-only overlay; publishing checks the source and weight revisions against
# the full source Dockerfile. Replace this tag with the verified OCI digest.
ARG API_BASE_IMAGE=dockermaker0/indextts25-api:sha-cc675a988153b9095b18eb1aa04336001628bc67
FROM ${API_BASE_IMAGE}
USER 0:0
COPY --chown=1000:1000 docker/audio-cpp/server.json /etc/indextts/server.json
USER 1000:1000
